AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: NBA Picks API — Flask backend on AWS Lambda + API Gateway

Globals:
  Function:
    Timeout: 120
    MemorySize: 512
    Runtime: python3.11
    Environment:
      Variables:
        USE_REAL_ODDS: "true"
        # ODDS_API_KEY: !Sub "{{resolve:ssm:/nba-picks/odds-api-key}}"
  HttpApi:
    CorsConfiguration:
      AllowOrigins:
        - "https://main.d1u82ndbyl4zb.amplifyapp.com"
        - "http://localhost:5173"
      AllowMethods:
        - GET
        - POST
        - OPTIONS
      AllowHeaders:
        - Content-Type
        - Authorization

Resources:
  NbaPicksFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: nba-picks-api
      CodeUri: .
      Handler: lambda_handler.handler
      Description: NBA player prop picks API
      Events:
        ApiRoot:
          Type: HttpApi
          Properties:
            Path: /
            Method: ANY
        ApiProxy:
          Type: HttpApi
          Properties:
            Path: /{proxy+}
            Method: ANY

Outputs:
  ApiUrl:
    Description: "API Gateway endpoint — use this as VITE_API_URL in Amplify"
    Value: !Sub "https://${ServerlessHttpApi}.execute-api.${AWS::Region}.amazonaws.com"
